#include<stdio.h>
#include<stdint.h>

int main(){

    int x;
    uint8_t y;
    uint16_t z;
    uint32_t a;
    uint64_t b;

    printf("%zu bytes\n",sizeof(X));
    printf("%zu bytes\n",sizeof(y));
    printf("%zu bytes\n",sizeof(z));
    printf("%zu bytes\n",sizeof(a));
    printf("%zu bytes\n",sizeof(b));

    return 0;
}